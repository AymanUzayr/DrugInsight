# DrugInsight

omg we are so cooked gng 🥀🥀


Requirement for users



Install:

Windows:

git lfs install

Linux:

sudo apt install git-lfs
git lfs install

Mac:

brew install git-lfs
git lfs install

Then clone normally.
